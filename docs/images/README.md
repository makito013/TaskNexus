# Imagens do README

Screenshots referenciadas pelo [README](../../README.md) principal:

- `hero-terminal.png` — terminal do TaskNexus em uso (ex.: sessão aberta a
  partir de um iPad via Tailscale).
- `board.png` — tela do Board (kanban).
- `tarefas.png` — tela de Tarefas (itens de acompanhamento registrados pelos
  agentes).

Estes arquivos ainda não existem neste repositório — captura pendente, a ser
feita a partir de uma instância local sem dados de projeto real.
